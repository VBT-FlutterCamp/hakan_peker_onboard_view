import 'package:flutter/material.dart';

class AppConstants {
  static const SizedBox sizedbox = SizedBox(height: 10);
  static const EdgeInsets containerMargin = EdgeInsets.only(right: 8, left: 8);
  static const double circleContainerHeight = 10;
  static const double circleContainerWidth = 10;
  static const BorderRadius circleBorderRadius = BorderRadius.all(Radius.circular(10));
  static const Duration buttonDuration = Duration(milliseconds: 500);
  static const int pageNumberOne = 1;
  static const int pageNumberTwo = 2;
  static const Curve pageCurve = Curves.decelerate;
}
